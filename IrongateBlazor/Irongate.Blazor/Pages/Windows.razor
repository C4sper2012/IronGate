@page "/windows"
@using uPLibrary.Networking.M2Mqtt
@using System.Diagnostics
@attribute [Authorize(Roles = "IronGate-User")]
@inject IConfiguration ConfigurationBuilder
@inject IWindowService WindowService


<h3>Windows</h3>

<RadzenSplitButton Click=@OnClickFirstFloor Text="First Floor">
    <ChildContent>
        <RadzenSplitButtonItem Text="Open" Value="1"/>
        <RadzenSplitButtonItem Text="Close" Value="0"/>
    </ChildContent>
</RadzenSplitButton>
<RadzenSplitButton Click=@OnClickGroundFloor Text="Ground Floor">
    <ChildContent>
        <RadzenSplitButtonItem Text="Open" Value="1"/>
        <RadzenSplitButtonItem Text="Close" Value="0"/>
    </ChildContent>
</RadzenSplitButton>

<RadzenSplitButton Click=@OnClickBasement Text="Basement">
    <ChildContent>
        <RadzenSplitButtonItem Text="Open" Value="1"/>
        <RadzenSplitButtonItem Text="Close" Value="0"/>
    </ChildContent>
</RadzenSplitButton>


@code {
    
    void OnClickFirstFloor(RadzenSplitButtonItem item)
    {
        string broker = ConfigurationBuilder["MosquittoBroker:WindowControls:FirstFloor:Broker"];
        int port =  Convert.ToInt32(ConfigurationBuilder["MosquittoBroker:WindowControls:FirstFloor:Port"]);
        string clientId = ConfigurationBuilder["MosquittoBroker:WindowControls:FirstFloor:ClientId"];
        string username = ConfigurationBuilder["MosquittoBroker:WindowControls:FirstFloor:Username"];
        string password = ConfigurationBuilder["MosquittoBroker:WindowControls:FirstFloor:Password"];
        string topic = ConfigurationBuilder["MosquittoBroker:WindowControls:FirstFloor:Topic"];
        
        WindowService.Init(broker, port, clientId, topic, username, password, item.Value);
    }

    void OnClickGroundFloor(RadzenSplitButtonItem item)
    {
        string broker = ConfigurationBuilder["MosquittoBroker:WindowControls:GroundFloor:Broker"];
        int port =  Convert.ToInt32(ConfigurationBuilder["MosquittoBroker:WindowControls:GroundFloor:Port"]);
        string clientId = ConfigurationBuilder["MosquittoBroker:WindowControls:GroundFloor:ClientId"];
        string username = ConfigurationBuilder["MosquittoBroker:WindowControls:GroundFloor:Username"];
        string password = ConfigurationBuilder["MosquittoBroker:WindowControls:GroundFloor:Password"];
        string topic = ConfigurationBuilder["MosquittoBroker:WindowControls:GroundFloor:Topic"];
        
        WindowService.Init(broker, port, clientId, topic, username, password, item.Value);
    }
    
    void OnClickBasement(RadzenSplitButtonItem item)
    {
        string broker = ConfigurationBuilder["MosquittoBroker:WindowControls:Basement:Broker"];
        int port =  Convert.ToInt32(ConfigurationBuilder["MosquittoBroker:WindowControls:Basement:Port"]);
        string clientId = ConfigurationBuilder["MosquittoBroker:WindowControls:Basement:ClientId"];
        string username = ConfigurationBuilder["MosquittoBroker:WindowControls:Basement:Username"];
        string password = ConfigurationBuilder["MosquittoBroker:WindowControls:Basement:Password"];
        string topic = ConfigurationBuilder["MosquittoBroker:WindowControls:Basement:Topic"];
        
        WindowService.Init(broker, port, clientId, topic, username, password, item.Value);
    }

}